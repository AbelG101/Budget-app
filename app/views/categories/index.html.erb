<p style="color: green"><%= notice %></p>

<h1 class="text-3xl font-bold underline">Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
</div>

<%= link_to "New category", new_category_path(user_id: current_user) %>
